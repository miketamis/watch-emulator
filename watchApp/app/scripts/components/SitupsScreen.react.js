/* @flow */
'use strict';

var React = require('react');

var SitupScreen = React.createClass({
  /**
   * @return {object}
   */
  render: function() {
      return (
      <div>
        Hello World
      </div>
    );
  }
});

module.exports = SitupScreen;
