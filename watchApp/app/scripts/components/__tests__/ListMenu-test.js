jest.autoMockOff();

describe('ListMenu', function () {
    var ListMenu = require('../ListMenu.react.jsx');

    //  var TestUtils = React.addons.TestUtils;


    it('it can\'t have no items', function () {
        //TestUtils.renderIntoDocument(< ListMenu/>);
    });
});
